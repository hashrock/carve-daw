次にやる (優先順)

- [x] 1. GUI から書き出し (Export WAV)
      今は src/app/ に Renderer が一度も出てこない。曲は作れるが自力で音声
      ファイルを出せず、CLI を叩くしかない。RenderMain.cpp に動く実装
      (mute を尊重する Renderer::Parameters 経路と、ジョブを直列実行する
      HeadlessUIBehaviour) があるので、メニュー項目と進捗表示を足すだけ
- [x] 2. ベロシティ編集
      setVelocity をアプリ側から一度も呼んでいない。ノートは固定値で作られ
      以後変えられないのに、ピアノロールは getVelocity() を明るさとして
      描画している = 見えるが変えられない値になっている。ドラムキットが
      入った以上ここは致命的 (ゴーストノートの無いハイハットは作れない)。
      ピアノロール下部のベロシティレーンが定石で、ルーラーを入れた時の
      構造がそのまま使える
- [x] 3. コピー&ペースト / パターン複製
      クリップボード処理がリポジトリ全体に存在せず、パターンを別スロットに
      複製する手段も無い (Meta+D はプレイリストのクリップのみ)。
      「パターンを作る→複製する→少し変える」が Orion 流の中核なので、
      触った時の印象を一番変えるのはここ

その次

- [x] テンポマーカーのドラッグで全 MIDI クリップが作り直される
      EditSync は SONG ツリーの変更を全再同期に回すので、レーン上で
      テンポをドラッグすると跨いだ拍ごとに全クリップが再構築される。
      ミキサーとループ範囲でやったのと同じく、位置だけ直す軽い経路が要る。
      クリップと配置を突き合わせるには、エフェクトチェーンでやった
      「モデルの id をプラグインに刻印する」のと同じ手が使える
- [x] マスターバス (マスターに insert を挿せない。tracktion の master
      plugin list を使う。ミックス全体に挿せないリミッターは意味が薄いので、
      リミッターが欲しかった件の本命はおそらくこれ)
- [ ] MIDI キーボード入力 (今は音のプレビューのみで、弾くことも録ることも
      できない。無効化してあるのはオーディオ入力で MIDI 入力は別系統なので、
      EngineSetup.h の CoreAudio バグ回避には当たらない)

小物だが効く

- [x] 拍子
- [x] テンポ変更 (途中変更に対応)
- [x] クオンタイズ / スウィング (スナップはあるが後からの補正手段が無い)
- [x] MIDI ファイルの読み書き
- [x] センド / リターン

全体

- [x] 編集中のファイル名とdirtyかどうかを表示
- [x] 上書き保存

Playlist

- [x] ペイントツールと選択ツールに分ける
- [x] ペイント的な挙動にする（左ドラッグで、空いていれば連続で選択中のパターンを置ける）
- [x] 左クリックでは生成・削除トグルになっているが、選択にする。ダブルクリックで編集
- [x] 右クリックドラッグで、連続してパターンを削除できる
- [x] パターン右上にドロップダウンでパターン変更
- [x] パターン右端ドラッグで、パターン長を調整可能
- [x] 上に小節数、ズーム、パンができるルーラースペース
- [x] ルーラーでループスタート・終了を指定できるように
- [x] 矩形選択
- [x] Meta + Dで複製
- [x] パターンにはA1-A9, B1-B9までの空きを事前に用意しておいて、追加せずとも無名パターンでもすぐ編集できるように
- [x] マウスカーソル種別を移動やリサイズで変える

Pianoroll

- [x] マウスカーソル種別を移動やリサイズで変える
- [x] クリック時に音プレビュー
- [x] ドラッグ移動時は、クリック位置からの移動差分で移動先を計算する
- [x] パターン長とグリッド単位を変えるUI
- [x] 上にルーラー
- [x] パターンのリネーム
- [x] 右クリックドラッグでノートの連続削除
- [x] Snap on / off トグルボタン

Mixer

- [x] ウィンドウサイズを自動計算
- [x] ウィンドウサイズを保存

Side chain

- [x] コンプのサイドチェイン入力に別トラックを送る
      (Rack 不要だった: graph エンジンが Plugin::sidechainSourceID を
      ネイティブサポートしており、EFFECT の sidechainSource プロパティ 1 個で済んだ)