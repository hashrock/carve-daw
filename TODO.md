# TODO

完了した項目は履歴にある (git log で追える)。ここには残作業と、
着手時に必要になる調査済みの事実だけを置く。

## 残作業

- [ ] ノートプレビューが「鳴ったり鳴らなかったり」する件の真因特定 (再現条件待ち)。
      shouldPlayMidiGuideNotes の有効化 (1c77f71) は本物の修正だが、その後の
      「空トラックでは LiveMidiInjectingNode が作られない」説は誤りだった:
      createNodeForClips はクリップ 0 件でも CombiningNode を返すので
      ラップは常に起きる (無パッチビルドの実機 A/B で確認済み)。
      コードから見える無音条件の候補:
      * ドラムキットの未割当パッド行 -- パッドは setKeyRange(note,note) の
        一音域なので、割当 16 行の外は仕様として無音 (最有力)
      * ミュート / 他トラックのソロ中 (ガイドノートは後段のミュートで落ちる)
      * audio ジェネレータ (楽器なし)
      対策候補: ドラムキットならロール側で割当パッド行をハイライトする

- [ ] MIDI キーボード入力
      今は音のプレビューのみで、弾くことも録ることもできない。
      無効化してあるのはオーディオ入力で MIDI 入力は別系統なので、
      EngineSetup.h の CoreAudio バグ回避 (README 参照) には当たらない
- [ ] モディファイアの編集 UI (エンジンとモデルは実装済み・.carve 直書きで動く。
      LFO の rate/depth/wave と ASSIGN を編集するパネルが無い)
- [ ] オートメーションのカーブベンド (PT.curve は保存・再生されるが
      レーン UI は直線描画のみ)
- [ ] オーディオ録音
      着手前に README の「録音に着手するときの注意」を読むこと

## 改善候補 (壊れてはいない)

- [ ] Pattern::getNumNotes() が NOTE を数えず子要素数を返す。
      PATTERN 配下に別種の子 (オートメーション等) を足すと誤カウントになる
- [ ] Playlist::addClip に長さ付きオーバーロードが無く、複製が
      add + setLength + setTranspose の 3 書き込み (毎回 refresh 再入)
- [ ] ピアノロールのノート明るさが 0.55+0.45*v/127 でベロシティ差が読みにくい
      (ベロシティレーンで編集は可能になったが、グリッド側の表示が圧縮されたまま)
- [ ] CLI レンダラーのサンプルレートがオーディオデバイス依存
      (RenderMain の sampleRateForAudio = deviceManager.getSampleRate())。
      GUI がデバイスを掴んでいるかどうかでレートが変わり、同じ曲でも
      44.1k/48k の別ファイルになるので、md5 比較での回帰チェックが空振りする。
      レートを固定するか、--rate オプションで指定できるようにしたい
