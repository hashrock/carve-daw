build/
build-*/
.DS_Store
*.wav
# ...except the factory kit, which is content the app ships rather than
# something a render left behind. Regenerate it with scripts/generate-drums.py.
!content/Drums/*.wav
.claude/
dist/
